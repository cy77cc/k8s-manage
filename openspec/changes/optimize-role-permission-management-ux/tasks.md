## 1. Role Management Entry Discoverability

- [ ] 1.1 在角色管理表格中新增显式操作列（至少包含“详情”“编辑权限”）并保留兼容行点击详情的辅助路径
- [ ] 1.2 统一 Roles 页面交互文案与按钮层级，确保首次进入即可识别权限编辑入口
- [ ] 1.3 为角色详情与权限编辑入口补充可访问性属性与空状态/加载状态反馈

## 2. Scalable Permission Editing Interaction

- [ ] 2.1 将权限选择器改造为“分组 + 搜索”结构，支持按模块/资源快速定位权限
- [ ] 2.2 实现批量操作能力：分组全选、筛选结果全选、反选、清空，并显示实时已选数量
- [ ] 2.3 在提交前展示本次变更摘要（新增/移除数量）并增加确认流程，提交后提供统一成功/失败反馈
- [ ] 2.4 对大数据量权限列表引入增量渲染或等效优化，保证滚动与勾选响应

## 3. API And Authorization Compatibility

- [ ] 3.1 评估现有角色权限更新接口是否可承载批量变更语义，必要时扩展 `grant[]/revoke[]` 或等效批量载荷
- [ ] 3.2 保证治理菜单与页面级操作按钮继续遵循角色感知可见性规则，仅授权用户可见并可操作
- [ ] 3.3 为新增或调整的权限更新请求补充后端参数校验与审计字段，保持 RBAC/Casbin 边界一致

## 4. Verification And Regression

- [ ] 4.1 新增/更新前端测试，覆盖显式入口可发现性、批量选择行为、提交确认与反馈
- [ ] 4.2 新增/更新后端测试，覆盖批量更新语义、未授权访问拒绝（HTTP 403）与审计记录
- [ ] 4.3 执行端到端回归：超级管理员可见且可编辑，普通角色不可见治理入口且无法直接访问治理 API
- [ ] 4.4 运行 `openspec validate --json` 并修复本变更产生的规范校验问题

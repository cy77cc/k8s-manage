## 1. 后端作用域与部署目标解析统一

- [x] 1.1 梳理并固定服务创建时 `project_id/team_id` 的权威来源，移除创建链路中的硬编码 `team_id` 写入逻辑
- [x] 1.2 在服务创建后端逻辑中增加作用域字段校验与缺失错误返回（明确缺失字段）
- [x] 1.3 收敛手动部署入口的目标解析为统一三段式顺序：显式指定 -> 服务默认目标 -> 作用域 fallback
- [x] 1.4 将 CI/CD 触发部署入口接入与手动部署相同的目标解析逻辑，避免分叉实现
- [x] 1.5 增强“deploy target not configured”错误诊断信息，包含 project/team/env/target_type 与修复建议
- [x] 1.6 为 fallback 命中场景补齐默认目标回填逻辑与必要保护，避免重复失败

## 2. 前端服务管理一致性修复

- [x] 2.1 在 `ServiceProvisionPage` 移除 `team_id` 硬编码提交，改为传递上下文推导结果或由后端兜底
- [x] 2.2 在 `ServiceListPage` 列表模式补齐“停止”行操作并与卡片操作保持一致
- [x] 2.3 为服务列表关键列（至少服务名、状态）补齐可排序行为
- [x] 2.4 将创建页环境等残留英文标签替换为中文展示，同时保持 API 枚举值兼容
- [x] 2.5 优化部署目标未配置时的前端提示，展示可执行修复指引

## 3. 测试与回归验证

- [x] 3.1 新增/更新后端单元测试：覆盖显式指定、默认目标、fallback 命中、fallback 失败四类场景
- [x] 3.2 新增/更新 CI 触发部署测试，验证其目标解析顺序与手动部署完全一致
- [x] 3.3 新增/更新前端交互测试：列表行操作完整性、排序行为、创建页中文化文案
- [x] 3.4 执行端到端联调：创建服务 -> 手动部署 -> CI/CD 触发部署，确认行为一致且错误可诊断

## 4. 迁移与发布保障

- [x] 4.1 编写历史数据检查脚本，识别受硬编码 team 影响的服务记录与默认目标缺失记录
- [x] 4.2 制定并执行灰度发布方案，开启关键日志观测目标解析命中路径与失败原因分布
- [x] 4.3 准备回滚预案：保留旧逻辑切换开关或回退步骤，并验证可在故障时快速恢复
- [x] 4.4 完成 OpenSpec 校验与变更说明更新，确保实现与 proposal/design/specs 一致

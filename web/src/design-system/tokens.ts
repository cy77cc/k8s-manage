/**
 * OpsPilot Design System Tokens
 *
 * 设计系统的基础 Token 定义，包括色彩、排版、间距、圆角、阴影、动画等。
 * 这些 Token 将被 Tailwind CSS 和 Ant Design 主题配置使用。
 */

// ============================================================================
// 色彩系统 (Color System)
// ============================================================================

export const colors = {
  // 主色调 (Primary Colors) - Indigo 色系
  primary: {
    50: '#eef2ff',
    100: '#e0e7ff',
    200: '#c7d2fe',
    500: '#6366f1',  // 主色
    600: '#4f46e5',  // hover
    700: '#4338ca',  // active
    900: '#312e81',  // 深色文本
  },

  // 中性色 (Neutral Colors) - Gray 色系
  gray: {
    50: '#fafbfc',   // 页面背景
    100: '#f8f9fa',  // 卡片背景
    200: '#e9ecef',  // 分隔线
    300: '#dee2e6',  // 边框
    400: '#ced4da',  // 禁用状态
    500: '#6c757d',  // 次要文本
    700: '#495057',  // 主要文本
    900: '#212529',  // 标题文本
  },

  // 语义色 (Semantic Colors)
  success: '#10b981',  // 成功状态（绿色）
  warning: '#f59e0b',  // 警告状态（橙色）
  error: '#ef4444',    // 错误状态（红色）
  info: '#3b82f6',     // 信息提示（蓝色）
};

// ============================================================================
// 排版系统 (Typography System)
// ============================================================================

export const typography = {
  // 字体家族
  fontFamily: {
    sans: [
      '-apple-system',
      'BlinkMacSystemFont',
      'Segoe UI',
      'PingFang SC',
      'Hiragino Sans GB',
      'Microsoft YaHei',
      'sans-serif',
    ].join(', '),
    mono: [
      'SF Mono',
      'Monaco',
      'Cascadia Code',
      'Consolas',
      'monospace',
    ].join(', '),
  },

  // 字号层级
  fontSize: {
    display: '32px',      // 2rem - 页面标题
    h1: '24px',          // 1.5rem - 一级标题
    h2: '20px',          // 1.25rem - 二级标题
    h3: '18px',          // 1.125rem - 三级标题
    bodyLarge: '16px',   // 1rem - 大号正文
    body: '14px',        // 0.875rem - 正文（默认）
    bodySmall: '13px',   // 0.8125rem - 小号正文
    caption: '12px',     // 0.75rem - 辅助文字
  },

  // 行高
  lineHeight: {
    tight: 1.25,    // 标题
    normal: 1.5,    // 正文（默认）
    relaxed: 1.75,  // 长文本
  },

  // 字重
  fontWeight: {
    regular: 400,   // 正文
    medium: 500,    // 强调
    semibold: 600,  // 小标题
    bold: 700,      // 标题
  },
};

// ============================================================================
// 间距系统 (Spacing System)
// ============================================================================

export const spacing = {
  xs: '4px',    // 0.25rem - 极小间距
  sm: '8px',    // 0.5rem - 小间距
  md: '16px',   // 1rem - 中等间距（默认）
  lg: '24px',   // 1.5rem - 大间距
  xl: '32px',   // 2rem - 超大间距
  '2xl': '48px', // 3rem - 页面级间距
  '3xl': '64px', // 4rem - 大区块间距
};

// ============================================================================
// 圆角系统 (Border Radius System)
// ============================================================================

export const borderRadius = {
  none: '0',
  sm: '4px',    // 小圆角（标签、徽章）
  md: '8px',    // 中等圆角（按钮、输入框）
  lg: '12px',   // 大圆角（卡片）
  xl: '16px',   // 超大圆角（模态框）
  full: '9999px', // 完全圆形（头像）
};

// ============================================================================
// 阴影系统 (Shadow System)
// ============================================================================

export const shadows = {
  sm: '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
  md: '0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px -1px rgba(0, 0, 0, 0.1)',
  lg: '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -2px rgba(0, 0, 0, 0.1)',
  xl: '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -4px rgba(0, 0, 0, 0.1)',
  '2xl': '0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 8px 10px -6px rgba(0, 0, 0, 0.1)',
};

// ============================================================================
// 动画系统 (Animation System)
// ============================================================================

export const animations = {
  // 持续时间
  duration: {
    fast: '150ms',    // 快速动画（悬停、焦点）
    normal: '250ms',  // 标准动画（默认）
    slow: '350ms',    // 慢速动画（页面切换）
    slower: '500ms',  // 更慢动画（复杂动画）
  },

  // 缓动函数
  easing: {
    standard: 'cubic-bezier(0.4, 0.0, 0.2, 1)',      // 标准缓动
    decelerate: 'cubic-bezier(0.0, 0.0, 0.2, 1)',    // 减速缓动（进入）
    accelerate: 'cubic-bezier(0.4, 0.0, 1, 1)',      // 加速缓动（退出）
    bounce: 'cubic-bezier(0.68, -0.55, 0.265, 1.55)', // 弹性缓动
  },
};

// ============================================================================
// 图标系统 (Icon System)
// ============================================================================

export const iconSizes = {
  xs: '12px',   // 极小图标（内联文本）
  sm: '14px',   // 小图标（按钮）
  md: '16px',   // 中等图标（默认）
  lg: '20px',   // 大图标（标题）
  xl: '24px',   // 超大图标（页面图标）
  '2xl': '32px', // 特大图标（空状态）
};

// ============================================================================
// 断点系统 (Breakpoint System)
// ============================================================================

export const breakpoints = {
  xs: '0px',      // 手机（竖屏）
  sm: '640px',    // 手机（横屏）
  md: '768px',    // 平板
  lg: '1024px',   // 笔记本
  xl: '1280px',   // 桌面
  '2xl': '1536px', // 大屏幕
};

// ============================================================================
// Z-Index 层级 (Z-Index System)
// ============================================================================

export const zIndex = {
  dropdown: 1000,        // 下拉菜单
  sticky: 1020,          // 固定元素
  fixed: 1030,           // 固定导航
  modalBackdrop: 1040,   // 模态框背景
  modal: 1050,           // 模态框
  popover: 1060,         // 弹出框
  tooltip: 1070,         // 工具提示
  notification: 1080,    // 通知
};

// ============================================================================
// 导出所有 Token
// ============================================================================

export const tokens = {
  colors,
  typography,
  spacing,
  borderRadius,
  shadows,
  animations,
  iconSizes,
  breakpoints,
  zIndex,
};

export default tokens;
